{
  "name": "public-practice-mono",
  "private": true,
  "version": "0.1.0",
  "packageManager": "pnpm@9",
  "workspaces": ["apps/*","packages/*"],
  "devDependencies": {
    "turbo": "^2.0.0"
  },
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev --parallel",
    "lint": "turbo run lint"
  }
}
